!(function (e, u) {
  "use strict";
  var t = {
    set: function (e, t, n, i, o, r) {
      e = e + "=" + t;
      n &&
        ((t = new Date()).setTime(t.getTime() + 24 * n * 60 * 60 * 1e3),
        (e += ";expires=" + t.toUTCString())),
        i && (e += ";domain=" + i),
        o && (e += ";path=" + o),
        r && (e += ";secure"),
        (u.cookie = e);
    },
    get: function (t) {
      var e = u.cookie
        .split(";")
        .map((e) => {
          e = e.trim().split("=");
          return { key: e[0], value: e[1] };
        })
        .find((e) => e.key === t);
      if (null != e) return e.value;
    },
    update: function (e, t, n, i, o, r) {
      this.set(e, t, n, i, o, r);
    },
    remove: function (e) {
      this.set(e, "", -1);
    },
    getAll: function (e) {
      return u.cookie.split(";").map((e) => {
        e = e.trim().split("=");
        return { key: e[0], value: e[1] };
      });
    },
    clear: function () {
      this.getAll().forEach((e) => this.remove(e.key));
    },
  };
  "function" == typeof define && define.amd
    ? define(function () {
        return t;
      })
    : "undefined" != typeof exports
    ? ("undefined" != typeof module &&
        module.exports &&
        (exports = module.exports = t),
      (exports.CookieManager = t))
    : (e.CookieManager = t);
})(window, document);
